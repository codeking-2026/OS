"""
User interface helpers for the OS simulator.

Example usage:
    from pyos.services.kernel import Kernel
    from pyos.ui.shell import Shell

    shell = Shell(Kernel())
    shell.onecmd("help")
"""
